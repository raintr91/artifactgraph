# ArtifactGraph — backend hooks

- Resolve endpoint, DTO, API, data, and business-rule tags from the local index.
- Run BE dry generation before write generation when both keys exist.
- Promote reviewed codegen/unit registry entries and templates in the BE repo.
- Leave `/api`, `/wire`, and `/unit` phase skills product-owned.
