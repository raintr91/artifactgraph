# ArtifactGraph — frontend hooks

- Resolve known shells, widgets, common components, and UI patterns from the local index.
- Run FE dry generation before write generation when both keys exist.
- Promote reviewed design/common registry entries and templates in the FE repo.
- Leave `/prototype`, `/wire`, `/unit`, and `/test` phase skills product-owned.
